{
  "name": "mgrin.eth",
  "version": "1.0.0",
  "source": "src/index.html",
  "author": "MGrin",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "parcel",
    "build": "parcel build --public-url https://mgrin.github.io",
    "deploy-ipfs": "ipfs-deploy dist -u pinata -p pinata"
  },
  "devDependencies": {
    "ipfs-deploy": "^12.0.0",
    "parcel": "^2.7.0"
  }
}
